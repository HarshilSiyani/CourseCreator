<div class="d-flex">
  <div id="course-sidebar" class="bg-light">
    <h5 class="text-small"><%= @course.name %></h5>
    <div id="topic-list" class="flex-grow-1">
      <ul>
        <% @course.study_modules.each do |study_module| %>
          <li><%= link_to study_module.name, edit_course_path(@course, study_module_id: study_module.id) %></li>
        <% end %>
        <li>
          <%= link_to  "Create a lesson", new_course_lesson_path(@course), class: "btn btn-info" %>
          <span>OR</span>
          <%= link_to  "Create a quiz", "#", class: "btn btn-info" %>
        </li>
      </ul>
    </div>
    <div id="settings">Settings</div>
  </div>
  <div class="modules bg-muted d-flex flex-column">
    <div class="editor-menu">
      <%= link_to  "Add Module", "#", class: "btn btn-primary" %> |
      <%= link_to  "Publish", "#", class: "btn btn-primary" %>
    </div>
    <div id="editor" class="flex-grow-1">
      <div id="editor-choices">
      </div>
    </div>
    <div class="course-description">
      <%= render "#{@contentable.class.table_name}/form", contentable: @contentable, course: @course %>
    </div>
    <div class="module-nav d-flex justify-content-between">
      <%= link_to  "Previous", '#', class: 'btn btn-lg btn-primary' %>
      <%= link_to  "Next", '#', class: 'btn btn-lg btn-primary' %>
    </div>
  </div>

</dixt
